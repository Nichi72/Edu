# DESIGN.md — 프로그래밍 교재 (Docs Light + Dark Code)

프로그래밍/게임 개발 학습 자료용 시각 언어. **본문은 밝은 문서형, 코드와 도표만 다크.**
장문을 스크롤로 읽는 화면이므로 가독성이 최우선이고, 다크 블록은 "코드/그림"이라는 신호로만 쓴다.

## 원칙
1. 캔버스는 항상 흰색. 회색 면(`#fbfbfc`)과 1px 경계선(`#ececef`)으로만 구획한다. 그림자는 쓰지 않는다.
2. 다크(`#121212`)는 **코드 블록과 그림(그래프)에만** 허용. 그 외 다크 카드 금지.
3. 액센트 그린은 기능적으로만 — 현재 위치, 링크, 정답, 진도, 섹션 번호. 배경 채우기로 쓰지 않는다.
4. 위계는 색이 아니라 **굵기와 명도**로 만든다. 본문 400, 강조 700. 중간 굵기 남용 금지.
5. 그라디언트, 이모지, 아이콘 장식, 둥근 말풍선 카드 금지.

## 색
```css
:root {
  /* 표면 */
  --bg:            #ffffff;   /* 페이지 */
  --bg-subtle:     #fbfbfc;   /* 보조 카드, 표 헤더, 사이드바 */
  --bg-muted:      #f4f4f5;   /* 인라인 코드, 라벨 칩 */
  --line:          #ececef;   /* 기본 경계선 */
  --line-strong:   #d4d4d8;   /* 선택된 요소 경계 */

  /* 텍스트 */
  --text:          #18181b;   /* 제목, 강조 */
  --text-body:     #3f3f46;   /* 본문 */
  --text-muted:    #52525b;   /* 보조 설명 */
  --text-subtle:   #71717a;   /* 캡션, 메타 */
  --text-faint:    #a1a1a9;   /* 라벨, 비활성 */

  /* 액센트 (그린) */
  --accent:        #1db954;   /* 마커, 체크, 진도 바 */
  --accent-text:   #157f3c;   /* 링크, 액센트 텍스트 (흰 배경 대비 확보) */
  --accent-deep:   #145c2c;   /* hover, 활성 목차 */
  --accent-bg:     #e8f5ec;   /* 배지, 활성 항목 배경 */
  --accent-bg-2:   #f1f7f2;   /* 정답/해설 블록 */

  /* 상태 */
  --warn-bg: #fff8ed;  --warn-line: #f0a12b;  --warn-text: #b1720a;
  --info-bg: #f1f6fd;  --info-line: #539df5;  --info-text: #2f6fbf;
  --err-bg:  #fdf2f3;  --err-line: #f2dcdf;   --err-text:  #c0454f;

  /* 코드 블록 (다크) */
  --code-bg:     #121212;
  --code-header: #1f1f1f;
  --code-text:   #cbcbcb;
  --code-key:    #7fb2f0;   /* 키워드 */
  --code-str:    #ffc16b;   /* 문자열, 속성 */
  --code-fn:     #7ee2a8;   /* 함수명 */
  --code-lit:    #ffffff;   /* 리터럴, 타입명 */
  --code-cmt:    #7c7c7c;   /* 주석 */
}
```

## 타입
- 서체: **Pretendard** (한글+라틴 한 벌). 코드는 `ui-monospace, SFMono-Regular, Menlo, monospace`.
- 굵기는 400 / 700 두 단계 + 제목 800. 그 사이는 쓰지 않는다.

| 역할 | 크기 / 행간 / 굵기 | 자간 |
|---|---|---|
| 장 제목 (h1) | 44px / 1.15 / 800 | −1.4px |
| 절 제목 (h2) | 28px / 1.3 / 700 | −0.7px |
| 절 번호 라벨 | 12px / 700 / 대문자 | 1.4px |
| 리드 문단 | 18px / 1.75 / 400 | — |
| 본문 | 16.5px / 1.8 / 400 | — |
| 카드·표 본문 | 14.5px / 1.65 | — |
| 코드 | 13.5px / 1.8 | — |
| 캡션·메타 | 12~13px / 400 | — |
| 섹션 라벨(UPPERCASE) | 11.5px / 700 | 1.2px |

- 숫자(호출 횟수, 이동 거리, 진도)는 `font-variant-numeric: tabular-nums`.
- 본문 단락에 `text-wrap: pretty`.

## 간격 · 형태
- 스케일 4 / 6 / 8 / 12 / 16 / 20 / 24 / 40 / 56.
- 섹션 사이 `56px`, 섹션 내부 요소 사이 `16px`, 카드 내부 `12~14px`.
- 반경: 인라인 코드 4px · 칩/라벨 5px · 카드/표 10px · 코드 블록 12px · 알약 버튼·태그 9999px.
- 본문 컬럼 최대 폭 **약 960px**(1680px 그리드 − 좌우 레일). 한글 기준 한 줄 약 58자.
  코드 블록과 그림이 가로 스크롤 없이 들어가야 해서 넓힌 값이다. 그 이상 넓히지 않는다.

## 레이아웃
```
[ 좌측 목차 292px ] [ 본문 (flex, 기준 900px) ] [ 우측 미니 목차 196px ]
```
- 좌측: 검색 필드 → 부(部) 라벨 → 장 목록. 현재 장은 `--accent-bg` 패널로 펼쳐 하위 절을 노출. 하단에 진도 바 고정.
- 상단: 60px 브레드크럼 바, `rgba(255,255,255,.86)` + `backdrop-filter: blur(10px)`.
- 우측: "이 페이지 내용" 앵커 목록, `position: sticky; top: 0`.
- **좁은 화면 대응**: 컨테이너 `flex-wrap: wrap`, 본문 `flex: 1 1 900px`, 우측 레일 `flex: 1 1 196px`로 두어 좁아지면 레일이 아래로 내려간다. 표는 `minmax()` 컬럼 + `overflow-x: auto`(절대 `hidden` 금지). 2단 그리드는 `repeat(auto-fit, minmax(300px, 1fr))`.

## 컴포넌트 규칙
**코드 블록** — 다크 배경, 상단 헤더 바(`--code-header`)에 그린 점 8px + 파일명(mono 13px, 흰색) + 경로(12px, `#7c7c7c`) + 우측 "복사"(11px 대문자, 자간 1.2px). 본문 `padding: 22px 24px`, 행간 1.8.

**표** — 헤더는 `--bg-subtle` + 11.5px 대문자 라벨. 행 구분은 `1px solid #f4f4f5`, 마지막 행은 선 없음. 첫 열은 mono 13.5px `--accent-deep`.

**콜아웃** — 좌측 3px 컬러 바 + 연한 배경. 라벨(대문자 11.5px)을 첫 줄에 놓고 본문 14.5px. 주의=warn, 팁=info, 오답=err.

**퀴즈 선택지** — 1px 경계 흰 카드, hover 시 `--bg-subtle`. 정답 `--accent-bg-2`/`#bfe0c9`, 오답 `--err-bg`/`--err-line`. 우측에 "정답/오답" 12.5px 700. 선택 즉시 채점하고 해설을 `--bg-subtle` 블록으로 펼친다.

**체크리스트** — 18px 라운드 5px 박스, 체크 시 `--accent` 채움 + 흰 ✓, 텍스트는 `--accent-text`.

**그림(그래프)** — 다크 패널(`--code-bg`, 반경 12px, 패딩 24px) 안에 배치. 캡션은 "그림 3-1 · 제목" 형식 14.5px 700 흰색 + 우측에 조건 설명 12px `#b3b3b3`.

**이전/다음 장** — 하단에 1px 경계 카드 2개, 좌우 정렬. 라벨 12px `--text-faint` + 제목 15px 700.

## 모션
- 트랜지션은 `.16s ease`, 대상은 `background-color`, `border-color`, `color`, 진도 바 `width .12s linear`.
- hover는 면을 한 단계 밝게/어둡게. 이동·확대·그림자 변화 없음.

## 카피 톤
- 간결한 설명체. 절 제목은 개념이 아니라 **문장**으로: "누가 언제 호출되는가", "프레임에 속도를 맡기지 않는다".
- 학습 목표는 "~할 수 있다"로 끝내고 번호를 붙인다.
- 메타는 중점으로 잇는다: `읽는 시간 약 25분 · 실습 2 · 문항 5`.
- 대문자 라벨은 구조 표시용(CHAPTER 03, TIP, 주의)에만.
