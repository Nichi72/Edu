# 수업 목차 — 플래피 버드를 만들면서 배운다

도움수준: 높음

<!--
교수용
- [x] 완료  |  [>] 진행 중  |  [ ] 예정. 수업 끝에 표시만 옮긴다. 날짜·주차는 쓰지 않는다.
- 도움수준: 높음 / 중간 / 낮음. 반이 잘 따라오면 내린다.
- 시연 절차, 먼저 겪게 할 것, 흔한 막힘, 분량은 guide.md 에 있다. 이 파일에는 두지 않는다.
- 항목 번호·제목·체크 문장은 guide.md 와 항상 같아야 한다. 한쪽만 고치지 않는다.
-->

**Unity 6 (6000.3.x) / 3D / URP / 카메라는 옆에서 고정**

하나의 게임을 학기 내내 키운다.

- `개념` 은 그 단계에서 쓸 수 있는 문법의 전부다. 튜터는 `[x]` 와 `[>]` 항목의 개념까지만 쓴다.
- `체크` 는 그 단계의 통과 기준이다. **학생이 도달해야 할 결론이지 알려줄 정보가 아니다.**

---

## 1부. 준비

- [ ] **00. 모두 똑같은 빈 프로젝트를 만든다**
  - 개념: Unity Hub / 프로젝트 템플릿 / 저장 경로 규칙 / 폴더 구조 / Project Settings 여는 법 / 코드 편집기 연결 / 에디터 창 이름
  - 체크 `cp-00`: Project Settings 의 Active Input Handling 이 `Both` 다. `Assets` 아래 폴더 다섯 개가 보인다. Hierarchy 에 Main Camera / Directional Light / Global Volume 이 있다. 씬 제목 표시줄에 `*` 가 없다

- [ ] **01. 튜터를 설치하고 말을 걸어본다**
  - 개념: `package` 폴더 배치 / `install.bat` 실행 / Unity 재시작 / 프로젝트 폴더에서 Claude Code 실행
  - 체크 `cp-01`: 일부러 낸 에러가 `.tutor/errors.md` 에 나타나고, 튜터에게 "무슨 에러 났어?"라고 물으면 방금 낸 에러를 짚는다. 튜터가 **완성 코드를 주지 않고 순서부터 묻는다**는 것을 학생이 확인한다

## 2부. 새를 띄운다

- [ ] **02. 화면과 좌표를 확정한다**
  - 개념: 씬 / 게임오브젝트 / 컴포넌트 / Transform(위치·회전·크기) / Orthographic 카메라와 `Size` / Game 뷰 화면 비율 / 씬 저장
  - 체크 `cp-02`: **Game 뷰에서** 새가 화면 왼쪽 가운데, 바닥이 아래에 보인다. 배경이 하늘색이고 Aspect 가 16:9 다. **카메라는 Position (0, 0, −10) 에 Orthographic `Size` = 5 다**(이 값이라야 09 의 천장 4.5 / 바닥 −3.5 가 화면과 맞는다). **바닥 윗면이 y = −4 라는 것을 Inspector 값으로 설명한다**(−4.5 + 스케일 1의 절반)

- [ ] **03. 색을 입힌다  [아트·기획]**
  - 개념: 머티리얼 만들기 / 셰이더가 무엇인지 한 줄 / 오브젝트에 드래그해서 적용
  - 체크 `cp-03`: 새와 바닥이 서로 다른 색이고, Game 뷰에서 한눈에 구분된다

- [ ] **04. 새에 첫 코드를 붙인다**
  - 개념: 스크립트 만들기 / 오브젝트에 붙이기 / `Start()` 와 `Update()` / `Debug.Log()` / **Console 창 읽는 법**
  - 체크 `cp-04`: **Collapse 를 끈 상태에서 Start 로그는 1줄, Update 로그는 계속 쌓이는 것**을 화면으로 보여주고, 그 차이를 자기 말로 설명한다

- [ ] **05. 새가 딱 한 칸 아래로 내려간다**
  - 개념: 변수와 자료형(`float`) / `Vector3` 를 `new` 로 만들기 / `transform.position` 에 통째로 대입하기(`=`)
  - 체크 `cp-05`: Play 를 누르면 새가 정확히 한 번 아래로 이동하고 멈춘다. Stop 하면 원래 자리로 돌아온다. **`=` 가 "같다"가 아니라 "넣는다"임을 자기 말로 설명한다**

- [ ] **06. 새가 계속 내려간다**
  - 개념: `Update()` 로 옮기기 / `+=` / 방향과 부호
  - 체크 `cp-06`: Play 후 **3초 안에** 새가 화면 아래로 사라진다. **Play 중 Inspector 의 Position Y 값이 매 프레임 같은 폭으로 줄어드는 것**을 보여준다

- [ ] **07. 숫자를 코드 밖에서 만지고, 어느 PC에서나 같게 만든다**
  - 개념: `public` 과 `private` / Inspector 노출 / 프레임의 의미 / `Time.deltaTime`
  - 체크 `cp-07`: 코드를 열지 않고 Inspector 로 낙하 속도를 바꾼다. **`targetFrameRate` 10 과 60 에서 각각 Play 했을 때 Console 에 찍힌 시간의 차이가 0.3초 이내다.** `deltaTime` 을 곱하는 이유를 자기 말로 설명한다

- [ ] **08. 스페이스를 누르면 위로 튄다**
  - 개념: `if` / `using UnityEngine.InputSystem;` / 입력 감지 `Keyboard.current.spaceKey.wasPressedThisFrame` / 누른 순간과 누르는 동안의 차이
  - 체크 `cp-08`: 스페이스를 연타해 새를 **5초 이상 화면 안에 유지**한다. 꾹 눌러도 한 번만 튀어오른다

- [ ] **09. 천장이나 바닥에 닿으면 죽는다**
  - 개념: 비교 연산자(`< > <= >=`) / **논리 연산자 `||` 와 `&&`** / `bool` / `!` / 상태에 따라 다르게 동작하기
  - 체크 `cp-09`: 천장 y=4.5 와 바닥 y=−3.5 에서 각각 새가 멈춘다. **새가 바닥 위에 얹혀 있고 파고들지 않는다.** 죽은 뒤 스페이스를 아무리 눌러도 반응이 없다. **Console 의 "Dead" 로그가 딱 한 번만 찍힌다**(계속 찍히면 죽은 뒤에도 판정이 도는 것이다)

- [ ] **10. 낙하를 직접 짠 코드에서 물리로 넘긴다**
  - 개념: `Rigidbody` 의 중력·질량 / **Constraints** / `GetComponent<>()` / `rb.linearVelocity` / `NullReferenceException`
  - 체크 `cp-10`: **새가 회전하지 않고**(Inspector 의 Rotation 이 (0,0,0) 에서 안 변한다) **Position Z 가 0 에서 변하지 않는다. 낙하가 등속이 아니라 점점 빨라진다(포물선을 그린다).** 스페이스를 눌렀을 때 위로 튀었다가 부드럽게 내려온다. **새가 덜덜 떨지 않는다.** 손으로 짠 낙하와 물리 중 무엇이 더 나은지, 왜 그런지 자기 말로 말한다

## 3부. 장애물을 만든다

- [ ] **11. 파이프 한 쌍이 흘러온다**
  - 개념: 새 오브젝트에 스크립트 붙이기 / **06~07 에서 썼던 이동 코드를 축만 바꿔 응용**(새는 10 에서 물리로 넘어갔지만, 그때 쓴 방식이 여기서 그대로 쓰인다) / **빈 게임오브젝트(Create Empty)를 껍데기로 쓰기** / 부모-자식 구조 / 자식의 Inspector 위치는 부모 기준(로컬)이라는 것
  - 체크 `cp-11`: 파이프가 x=11 에서 출발해 **약 7~8초 뒤** 화면 왼쪽 밖으로 완전히 사라진다. **부모 하나만 움직이는데 위아래가 함께 간다. 자식 둘의 Inspector 에 이동 스크립트가 없다.** 06~07 에서 쓴 새의 낙하 코드와 뭐가 같고 뭐가 다른지 말한다

- [ ] **12. 파이프를 설계도로 만들어 복제한다**
  - 개념: **프리팹** / 프리팹과 씬 인스턴스의 차이 / `Instantiate()` / **Inspector 로 참조 연결하기(`public GameObject`)**
  - 체크 `cp-12`: Play 하면 씬에 없던 파이프가 x=11 에 생겨 흘러간다. Hierarchy 에 `PipePair(Clone)` 이 보이고 Stop 하면 사라진다. **프리팹을 한 번 수정하면 이후 생기는 파이프가 전부 바뀐다**

- [ ] **13. 파이프가 몇 초마다 계속 나온다**
  - 개념: 시간 재기(변수 누적 + `if` + 리셋) / `Time.deltaTime` 의 재사용
  - 체크 `cp-13`: 파이프가 **눈으로 세어 2초 간격**으로 나오고 파이프 사이 거리가 일정하다. Inspector 에서 `Spawn Interval` 을 1로 바꾸면 촘촘해진다. **"간격 × 속도 = 파이프 사이 거리"** 를 자기 말로 설명한다

- [ ] **14. 지나간 파이프를 치운다**
  - 개념: `Destroy()` / 위치 조건 판정 / `gameObject` 와 `this` 의 차이 / **Play 옆의 Pause · Step 버튼**
  - 체크 `cp-14`: **3분 플레이한 뒤 Pause 를 걸고 Hierarchy 의 `PipePair(Clone)` 개수를 세면 5개 이하로 유지된다.** 화면 안에서 파이프가 사라지는 일은 없다

- [ ] **15. 파이프 높이가 매번 달라진다**
  - 개념: `Random.Range` 와 범위 / **클래스와 인스턴스**(같은 스크립트를 붙여도 각자 자기 값을 가진다)
  - 체크 `cp-15`: **14 에서 쓴 Pause 버튼을 누르고** Hierarchy 에서 파이프 세 개를 차례로 클릭해 Inspector 의 Position Y 를 비교한다. **세 값이 서로 다르다.** 두 번 플레이했을 때 지형이 다르다

- [ ] **16. 시작하자마자 파이프가 깔려 있다**
  - 개념: **`for` 반복문** / 시작값·증가폭·반복 횟수 / **숫자를 그대로 박지 않고 계산해서 쓰기** / 다른 오브젝트가 가진 값 하나 꺼내 쓰기(`GetComponent<PipeMove>().speed` — 10 에서 배운 `GetComponent<>()` 를 다시 쓴다)
  - 체크 `cp-16`: Play 하면 파이프 두 개가 화면 안에 이미 보인다. **`for` 의 반복 횟수(`count`)만 바꾸면 파이프 개수가 바뀐다.** 미리 깔린 마지막 파이프와 첫 스폰 파이프 사이의 간격이 다른 구간과 눈에 띄게 다르지 않다. **Inspector 에서 `Spawn Interval` 을 1.5 로 바꿔 Play 해도, 프리팹 `PipePair` 의 `Speed` 를 4 로 바꿔 Play 해도 그 간격이 여전히 같다**(확인이 끝나면 2 와 3 으로 되돌린다)

- [ ] **17. 파이프 높이를 우리가 짠 순서대로 내보낸다  [아트·기획]**
  - 개념: 배열 / 인덱스 / `.Length` / **값을 돌려주는 메서드(`return`)** / (곁들여) `List` 가 무엇인지 한 줄
  - 체크 `cp-17`: 두 번 플레이했을 때 지형 순서가 **같다.** 배열 값을 바꾸면 그 순서대로 나온다. **배열을 5개에서 7개로 늘려도 코드의 다른 곳을 고칠 필요가 없다**(`.Length` 를 썼기 때문이다)

## 4부. 죽음 처리와 충돌

- [ ] **18. 죽으면 화면 전체가 멈춘다**
  - 개념: **중앙 관리 오브젝트(`GameManager`)** / `FindFirstObjectByType<T>()` / **`void` 메서드로 묶기** / 여러 스크립트가 한 값을 함께 보기 / **같은 처리가 두 번 돌지 않게 막기** / Inspector 로 컴포넌트 참조 연결하기
  - 체크 `cp-18`: **죽은 뒤 10초를 기다려도 새 파이프가 생기지 않고, 화면의 파이프가 한 픽셀도 움직이지 않는다.** Console 의 "Game Over" 가 한 번만 찍힌다. **`GameManager` Inspector 의 `Bird Rb` 칸에 `Bird` 가 들어가 있고, 죽으면 새도 그 자리에 멈춘다**

- [ ] **19. 파이프에 부딪히면 죽는다**
  - 개념: `Collider` / 충돌 이벤트가 오려면 한쪽에 `Rigidbody` 가 있어야 한다(**10 에서 이미 붙였다**) / `OnCollisionEnter` / **태그 만들기와 지정하기(두 단계)** / `CompareTag` / `isKinematic`
  - 체크 `cp-19`: **파이프에 닿으면 Console 에 파이프 로그, 바닥에 닿으면 바닥 로그가 각각 한 번만 찍힌다.** 두 경우 모두 새가 그 자리에서 멈추고 파이프도 멈춘다

## 5부. 점수와 게임 흐름

- [ ] **20. 파이프를 통과하면 점수가 오른다**
  - 개념: **`Is Trigger`** / `OnTriggerEnter` / 충돌과 트리거의 차이 / `int` 증가 / **눈에 안 보이는 판정용 오브젝트**
  - 체크 `cp-20`: **파이프 3개를 통과하면 Console 에 정확히 1, 2, 3 이 찍힌다.** 통과할 때만, 한 번만 오른다. **파이프에 부딪혀 죽었을 때는 점수가 오르지 않는다.** 새가 트리거 영역에서 멈추거나 튕기지 않는다

- [ ] **21. 점수가 화면에 보인다**
  - 개념: Canvas / **TextMeshPro** / Canvas Scaler / 앵커 / `using TMPro;` / 참조 연결(다시) / 숫자를 문자열로 바꾸기(`ToString()`)
  - 체크 `cp-21`: 파이프를 통과하는 **순간 화면 숫자가 바뀐다. Game 뷰 Aspect 를 16:9 에서 4:3 으로 바꿔도 텍스트가 화면 밖으로 나가지 않는다**(확인이 끝나면 16:9 로 되돌린다)

- [ ] **22. 죽으면 다시 시작할 수 있다**
  - 개념: 씬 다시 불러오기(`SceneManager.LoadScene`) / `using UnityEngine.SceneManagement;` / `PlayerPrefs` / **`SetActive` 로 오브젝트 켜고 끄기** / `GameObject` 와 `TextMeshProUGUI` 의 차이
  - 체크 `cp-22`: **죽음 → 게임오버 문구가 화면에 뜸 → 스페이스 → 점수 0, 새는 처음 위치. 이 순환을 3회 연속 돌린다.** **화면 위의 `Best` 숫자가 재시작 후에도 그대로 남아 있고, 에디터를 껐다 켠 뒤 Play 해도 그대로 보인다**

- [ ] **23. 시작 화면에서 기다렸다가 시작한다**
  - 개념: **`enum`** / 여러 파일에 걸친 이름 바꾸기 / **물리와 상태의 연결**
  - 체크 `cp-23`: **대기 화면에서 새가 떨어지지 않고 15 에서 미리 깔아둔 파이프도 흘러가지 않는다 → 스페이스로 시작 → 죽으면 화면이 멈추고 게임오버 문구가 뜬다 → 스페이스로 재시작하면 점수가 0 이고 새가 처음 위치에 있다. 이 순환을 3회 연속 돌린다**

- [ ] **24. 소리를 넣고 난이도를 맞춘다**
  - 개념: **소리 파일 준비** / `AudioSource` / `AudioClip` / `PlayOneShot` / **매개변수가 있는 메서드** / 지금까지 만든 값들을 조율하기
  - 체크 `cp-24`: 점프·점수·충돌에서 각각 다른 소리가 난다. **자기가 아닌 다른 사람이 3판 안에 5점 이상**을 낸다. 바꾼 값과 그 이유를 한 줄씩 적었다

## 6부. 내 것으로 만든다

- [ ] **25. 겉모습을 내 것으로 바꾼다  [아트·기획]**
  - 개념: 머티리얼 교체 / 3D 모델을 씬에 넣되 **콜라이더 구조는 유지하기** / 배경색·조명 / (원하면) 스카이박스
  - 체크 `cp-25`: 화면이 눈에 띄게 달라졌는데 **게임은 그대로 동작한다**(5점까지 플레이해서 확인)

- [ ] **26. 실행 파일로 만들어 남에게 준다**
  - 개념: `File > Build Profiles` / Scene List / 해상도 설정 / 결과물 압축
  - 체크 `cp-26`: **빌드한 실행 파일을 다른 사람 PC 에서 Unity 없이 실행해 5점 이상을 낸다**


