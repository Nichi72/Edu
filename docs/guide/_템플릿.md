# cp-XX. <제목>

<!--
─── 집필 메모 (학생에게 안 보이는 부분) ───
상태:     초안 | 확정 | 재검토필요
근거커밋: <이 챕터를 쓴 시점의 커밋 해시>
의존값:   <이 챕터가 기대는 숫자. 예: 파이프 속도 3, Ortho Size 5, 화면 왼쪽 끝 x=-8.89>
          뒤 항목에서 이 값이 바뀌면 이 챕터는 '재검토필요' 가 된다
대체예정: <뒤에서 이 코드가 갈아엎히면 적는다. 예: cp-18 에서 물리로 교체됨>

─── 독자 ───
Unity 도 프로그래밍도 처음인 학생. 혼자 읽고 진도를 뺄 수 있어야 한다.
- "Hierarchy 에서 우클릭" 을 모른다고 전제한다. 메뉴 경로는 전부 적는다
- 처음 나오는 용어는 반드시 「알아야 할 것」에서 먼저 설명한다. 설명 없이 쓰지 않는다
- 영어 용어는 처음 나올 때 한 번 "무슨 뜻인지" 를 붙인다
- 문장은 짧게. 한 문단 3줄 넘기지 않는다
-->

> **이번 목표** — <한 줄. Play 를 눌렀을 때 화면에서 무엇이 달라지는가>

<!-- 완성 화면 스크린샷 -->
![이번 항목을 마치면 이렇게 된다](../shots/cp-XX/done.png)

---

## 시작하기 전에

앞 항목까지 이게 되어 있어야 한다. 하나라도 아니면 앞으로 돌아가자.

- [ ] <직전 상태 1>
- [ ] <직전 상태 2>

---

## 알아야 할 것

<이번에 **처음 나오는 것 전부**. 하나도 빠뜨리지 않는다.
 "이건 당연히 알겠지" 가 이 교재에서 가장 위험한 생각이다.

 용어 하나당 이 순서로 쓴다:
   1. 한 줄 정의
   2. 비유 (일상에서 아는 것에 빗대기)
   3. 이 게임에서 어디에 쓰이나
   4. 초보가 오해하는 지점 (있으면)   >

### <용어 1>

<한 줄 정의>

> **비유** — <일상 비유>

<이 게임에서 어디에 쓰이나>

⚠️ **헷갈리기 쉬운 것** — <오해 지점>

### <용어 2>

...

---

## 해보기

### 1. 순서부터 세운다 — 코드는 아직 보지 말자

<무엇을 만들 건지 다시 한 줄로 말해주고, 학생이 직접 쓰게 한다>

**컴퓨터한테 시킨다고 생각하고, 무슨 일이 어떤 순서로 일어나야 하는지 한국어로 적어보자.**
한 줄에 하나씩, <N>줄이면 된다. 틀려도 된다.

```
1. ________________________________
2. ________________________________
3. ________________________________
```

<막혔을 때를 위한 사다리 — 답을 주지 말고 범위를 좁혀준다>

> 하나도 안 떠오르면: **제일 먼저 일어나는 일 하나만** 생각해보자. <힌트 질문>

**이게 왜 중요한가** — <이 단계를 건너뛰면 왜 안 되는지. 매 챕터 반복해서 각인시킨다>

### 2. 에디터에서 준비하기

<클릭 단위. 메뉴 경로는 `>` 로 전부. 창 이름도 적는다.
 스크린샷이 필요한 지점에 이미지를 넣는다>

1.
2.

### 3. 코드 채우기

<빈칸 = 1단계에서 쓴 순서의 줄 수. 힌트에 API 이름까지 주되 **답은 적지 않는다**>

`Assets/Scripts/<파일명>.cs`

```csharp
```

---

## 확인하기

**이렇게 되면 성공이다.**

<syllabus 의 `체크` 문장을 학생 말로 옮긴다. 조건 하나당 한 줄>

- [ ]
- [ ]

<설명 게이트 항목(cp-02·03·05·09·18·26)이면 여기에 "말로 설명해보기" 를 넣는다>

---

## 안 되면

<**내가 실제로 겪은 것만** 적는다. 상상해서 쓰지 않는다.
 증상 → 원인 → 어떻게 고치나 순서로>

### <증상>

<원인 설명 — 왜 이런 일이 생기는지 이해시킨다. "이렇게 하면 된다" 만 쓰면 다음에 또 막힌다>

**고치는 법**:

---

## 그래도 안 되면 — 답

> **여기까지 스스로 해봤다면 봐도 된다.**
> 순서를 안 세우고 여기부터 보면 다음 항목에서 더 크게 막힌다. 이건 겁주는 말이 아니라 실제로 그렇다.

**순서 예시** (정답은 아니다. 이렇게 쪼갤 수도 있다는 예시다)

1.
2.

**완성 코드**

```csharp
```

<코드 각 줄이 위 순서의 몇 번인지 대응시켜 설명>

---

## 더 알고 싶으면

<선택. 궁금해할 만한 것 한두 개.
 다음 항목 예고는 "지금 이게 불편하지? 다음에 해결한다" 형태로 —
 **깨달아야 할 결론을 미리 말하지 않는다**>

---
---

<!--
═══════════════════════════════════════════════
 교수님께 — 학생용 교재에는 안 들어가는 부분
 (Phase 3 에서 guide.md 로 뽑아낸다)
═══════════════════════════════════════════════

## 먼저 겪게 할 것
<학생이 스스로 부딪혀야 하는 실패. 튜터·교수가 미리 말하면 이 항목의 학습이 사라지는 것>

## 시연 절차
<수업에서 앞에 띄워놓고 보여줄 순서>

## 분량
- 내가 걸린 시간:
- 학생 예상(초보 기준):
- 수업 배분 의견:

## 검증 기록
| 체크 문장의 조건 | 결과 | 근거 (실측값·로그·스크린샷) |
|---|---|---|
|  |  |  |

검증 방법:
실측값 (뒤 항목이 참조할 숫자):

## 커밋
`<부> cp-XX - <제목>` / 태그 `cp-XX`
-->
