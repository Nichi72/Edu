# cp-XX — <syllabus 제목 그대로>  (N부)

<!--
상태: 초안 | 확정 | 재검토필요
근거커밋: <이 챕터를 쓴 시점의 커밋 해시>
의존값: <이 챕터가 기대는 숫자들. 예: 파이프 속도 3, 스폰 간격 2, Ortho Size 5>
         → 이 값이 뒤 항목에서 바뀌면 이 챕터는 '재검토필요' 가 된다
-->

> **이건 교재 챕터다. 항목을 만들자마자 그 자리에서 쓴다.**
> 나중에 몰아 쓰면 "어디서 막혔는지"가 기억에서 사라지고, 결국 상상으로 쓰게 된다.

## 만드는 것
<한 줄. 학생이 이 항목을 끝내면 화면에서 무엇이 달라지나>

## 직전 상태
<cp-(XX-1) 까지 끝냈을 때 화면에서 뭐가 되고 있나. 학생이 이어받는 지점>

## 쓸 수 있는 개념
<이번에 **새로 열린 것**을 굵게. 누적 목록은 `docs/개념누적.md`>

**이번 항목에서 쓰면 안 되는 것**: <유혹이 될 만한 뒤 개념과 그 항목 번호>

---

## 시연 절차
<클릭 단위로. 초보는 "Hierarchy 에서 우클릭"부터 모른다.
 메뉴 경로는 `>` 로 전부 적는다. 창 이름도 적는다>

1.
2.

## 먼저 겪게 할 것
<학생이 스스로 부딪혀야 하는 실패. **튜터가 미리 말하면 이 항목의 학습이 사라지는 것.**
 syllabus 의 `체크` 문장이 요구하는 "도달해야 할 결론"이 여기 걸린다>

## 흔한 막힘
> **실제로 겪은 것만 적는다. 상상해서 쓰지 않는다.**
> 이게 만들면서 교재를 쓰는 유일한 이유다. 없었으면 "없음"이라고 적는다.

| 증상 | 원인 | 어떻게 짚어줄까 |
|---|---|---|
|  |  |  |

## 분량
- 내가 걸린 시간:
- 학생 예상(초보 기준):
- 수업 배분 의견:

---

## 완성 코드
```csharp
```

**직전 대비 변경점**
```diff
```

## 학생 "순서" 모범 답안
<한국어 3~4줄. 학생이 이 정도를 써내면 2단계(빈칸)로 넘어간다.
 `CLAUDE_Test_.md` 1단계용>

1.
2.

**부실할 때 되물을 말**:

## 빈칸 코드
<빈칸 수 = 위 순서의 줄 수. 힌트에 API 이름까지 (도움수준 '높음' 기준).
 **힌트 안에 답을 적지 않는다.** `CLAUDE_Test_.md` 2단계용>

```csharp
```

---

## 검증 기록
syllabus `체크 cp-XX` 를 **조건 단위로 쪼개서** 각각 O/X 와 근거를 남긴다.
이 표가 "이 챕터를 믿어도 되는 이유"다.

| 체크 문장의 조건 | 결과 | 근거 (실측값·로그·스크린샷) |
|---|---|---|
|  |  |  |

**검증 방법**: <쓴 MCP 툴 / Roslyn 스니펫 요약>

**실측값**: <뒤 항목이 참조할 수 있는 숫자. 화면 경계, 소요 시간 등>

## 스크린샷
| 파일 | 무엇을 보여주나 |
|---|---|
| `../shots/cp-XX/game.png` | Game 뷰 |

## 커밋
`<부> cp-XX - <제목>` / 태그 `cp-XX`
